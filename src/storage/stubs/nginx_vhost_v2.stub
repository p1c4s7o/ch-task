server {
    listen {{PORT}};
    server_name {{DOMAIN_NAME}}.localhost;
    location / {
        root /var/www/html/{{DOMAIN_NAME}}.{{PORT}};
        index index.html;
        try_files $uri $uri/ =404;
    }
}
