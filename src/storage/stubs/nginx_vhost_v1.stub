server {
    listen 80;
    server_name {{DOMAIN_NAME}}.localhost;
    root /var/www/html/{{DOMAIN_NAME}}.localhost;

    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
